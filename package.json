{
  "name": "bqurtas-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-assets.mjs",
    "check:syntax": "node --check preview_site/js/main.js && node --check preview_site/js/gallery.js && node --check preview_site/js/i18n.js && node --check preview_site/js/enhance.js && node --check functions/api/2fa.js && node --check 'functions/[[route]].js'",
    "validate": "node scripts/validate-site.mjs",
    "test": "npm run check:syntax && npm run validate && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "terser": "5.44.0"
  }
}
