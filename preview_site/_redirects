# Send www to the canonical apex host (bqurtas.com)
https://www.bqurtas.com/*  https://bqurtas.com/:splat  301!

# Some scanners and clients still look for the legacy root path.
/security.txt  /.well-known/security.txt  301

# SPA fallback — serve the app shell for client-side routes so deep-links
# (/design, /design/logo, /blog, /bio, /contact) load even if the Pages
# Function isn't active. Static files and Functions take precedence over this.
/*  /index.html  200
