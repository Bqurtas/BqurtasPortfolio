!function(){"use strict";if("undefined"!=typeof IntersectionObserver){var e=matchMedia("(prefers-reduced-motion: reduce)").matches;window.__bqRouteCurtainCoverMs=680;var t,n=new IntersectionObserver(function(e){e.forEach(function(e){e.isIntersecting&&(e.target.classList.add("is-color"),n.unobserve(e.target))})},{threshold:.12,rootMargin:"0px 0px -7% 0px"});m();var o=document.getElementById("grid");o&&new MutationObserver(m).observe(o,{childList:!0,subtree:!0});var r=new IntersectionObserver(function(e){e.forEach(function(e){e.isIntersecting&&(e.target.classList.add("lux-in"),r.unobserve(e.target))})},{threshold:.12,rootMargin:"0px 0px -6% 0px"}),a=[".section-head",".bio-teaser-grid",".logos-head",".latest-blog-head",".room-features",".room-hero-inner",".bio-block",".pencemor-hero-inner",".feature-card",".tab-header"].join(",");if(document.querySelectorAll(a).forEach(function(e){e.classList.add("lux-reveal"),r.observe(e)}),document.addEventListener("click",function(e){e.target.closest("[data-route]")&&setTimeout(function(){document.querySelectorAll(".room:not(.is-hidden) .lux-reveal:not(.lux-in)").forEach(function(e){e.getBoundingClientRect().top<innerHeight&&e.classList.add("lux-in")})},90)},!0),!e){var i=document.createElement("div");i.className="lux-transition",i.setAttribute("aria-hidden","true"),i.innerHTML='<span class="lux-transition-room">Design</span><span class="lux-transition-no">01 / 06</span><span class="lux-transition-line"></span>',document.body.appendChild(i);var s={design:"01 / 06",work:"02 / 06",brandboard:"03 / 06",blog:"04 / 06",bio:"05 / 06",contact:"06 / 06"},c={design:"Design",work:"Selected Work",brandboard:"The Brand Board",blog:"The Journal",bio:"The Designer",contact:"Let's talk."},u=!1,d=function(e){if(u)return!0;if(e&&e===document.body.dataset.room)return!1;var t=document.querySelector('.mm-link[data-route="'+e+'"] .mm-link-text'),n=t?t.textContent.trim():c[e],o=i.querySelector(".lux-transition-room"),r=i.querySelector(".lux-transition-no");return o&&(o.textContent=n||c.design),r&&(r.textContent=s[e]||s.design),u=!0,i.classList.remove("is-leaving"),i.classList.add("is-active"),setTimeout(function(){i.classList.add("is-leaving")},900),setTimeout(function(){i.classList.remove("is-active","is-leaving"),u=!1},1660),!0};window.__bqPlayRoomCurtain=d,document.addEventListener("click",function(e){var t=e.target.closest("[data-route]");if(t){var n=t.getAttribute("data-route");d(n)}},!0),addEventListener("pagehide",function(){i.classList.remove("is-active","is-leaving")},{once:!0})}!function(){var e=document.querySelector(".logo-marquee"),t=e&&e.querySelector(".logo-marquee-track");if(e&&t&&t.getAnimations){var n=function(e){t.getAnimations().forEach(function(t){t.updatePlaybackRate?t.updatePlaybackRate(e):t.playbackRate=e})};e.addEventListener("mouseenter",function(){n(.22)}),e.addEventListener("mouseleave",function(){n(1)})}}()}function l(){document.querySelectorAll(".card:not([data-lux])").forEach(function(t){t.dataset.lux="1",t.classList.add("lux-gray"),e?t.classList.add("is-color"):n.observe(t)})}function m(){clearTimeout(t),t=setTimeout(l,280)}}();
/* ---- soft lerp scroll (desktop, fine pointers) — the slow, buttery Framer
   feel, FAIL-OPEN: any error or stall instantly returns native scrolling. ---- */
(function(){
  if (!matchMedia('(min-width:821px) and (pointer:fine)').matches) return;
  var target = 0, cur = 0, raf = null, active = false, dead = false;
  var lastMove = 0, lastY = -1;
  var EASE = 0.09;
  document.documentElement.style.setProperty('scroll-behavior','auto','important');
  function maxY(){ return Math.max(0, (document.scrollingElement || document.documentElement).scrollHeight - innerHeight); }
  function loop(){
    try{
      cur += (target - cur) * EASE;
      if (Math.abs(target - cur) < 0.5){
        cur = target;
        window.scrollTo({ top: cur, behavior: 'auto' });
        raf = null; active = false; return;
      }
      window.scrollTo({ top: cur, behavior: 'auto' });
      /* watchdog: if the page position refuses to move while we hold the wheel,
         hand scrolling back to the browser for good */
      var y = window.scrollY;
      if (y !== lastY){ lastY = y; lastMove = performance.now(); }
      else if (performance.now() - lastMove > 600 && Math.abs(target - cur) > 4){
        dead = true; raf = null; active = false; return;
      }
      raf = requestAnimationFrame(loop);
    }catch(err){ dead = true; raf = null; active = false; }
  }
  addEventListener('wheel', function(e){
    try{
      if (dead) return;
      if (e.ctrlKey || e.metaKey || e.shiftKey) return;              /* zoom / horizontal */
      if (document.body.classList.contains('menu-open')) return;    /* overlay owns input */
      var t = e.target;
      if (t && t.closest && t.closest('.chat, .latest-panel, .mobile-menu, .mm-right, textarea, select, .bb-mount, .reader, .lb, .mobile-sheet, [data-native-scroll]')) return;
      var d = e.deltaY;
      if (e.deltaMode === 1) d *= 16; else if (e.deltaMode === 2) d *= innerHeight;
      if (!isFinite(d) || d === 0) return;
      e.preventDefault();
      if (!active){ cur = window.scrollY; target = cur; active = true; lastY = -1; lastMove = performance.now(); }
      target = Math.max(0, Math.min(maxY(), target + d));
      if (!raf) raf = requestAnimationFrame(loop);
    }catch(err){ dead = true; }
  }, { passive:false });
  /* stay in sync when scrolling happens by other means (keys, glide, anchors) */
  addEventListener('scroll', function(){
    if (!raf){ target = window.scrollY; cur = target; }
  }, { passive:true });
})();

(function(){var f=document.getElementById('faCss');if(f)f.media='all';})();

/* ---- the hover tab-card is a LINK: it stays while hovered and a click
   activates the tab it previews ---- */
(function(){
  var last = null;
  document.addEventListener('mouseover', function(e){
    if (!e.target || !e.target.closest) return;
    var t = e.target.closest('.tabs .tab');
    if (t) last = t;
    var c = e.target.closest('.tab-card');
    if (c) c.classList.add('is-shown');
  }, true);
  document.addEventListener('click', function(e){
    if (!e.target || !e.target.closest) return;
    var c = e.target.closest('.tab-card');
    if (c && last){ c.classList.remove('is-shown'); last.click(); }
  }, true);
})();

/* ---- desktop: switching tabs scrolls the view JUST past the room intro
   (its bottom edge meets the viewport top), so "01 — Design Room / A small
   catalogue…" — the lede line included — sits fully under the scroll and the
   tab header leads. Scrolling up still reveals the intro. ---- */
(function () {
  if (!matchMedia('(min-width:821px)').matches) return;
  document.addEventListener('click', function (e) {
    var t = e.target && e.target.closest && e.target.closest('.tabs .tab');
    if (!t) return;
    var sec = t.closest('.section');
    if (!sec) return;
    setTimeout(function () {
      var intro = sec.querySelector('.section-head');
      var header = sec.querySelector('.tab-header') || sec.querySelector('#grid');
      var y;
      if (intro && intro.offsetHeight > 0) {
        y = window.scrollY + intro.getBoundingClientRect().bottom + 2;
      } else if (header) {
        y = window.scrollY + header.getBoundingClientRect().top - 8;
      } else { return; }
      window.scrollTo({ top: Math.max(0, y), behavior: 'smooth' });
    }, 250);
  }, true);
})();
